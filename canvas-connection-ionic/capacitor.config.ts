import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'canvas-connection-ionic',
  webDir: 'www'
};

export default config;
